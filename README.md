# Roar backend

This repository contains the backend API for deployment to a Render web service.

## Run locally

```bash
npm install
npm start
```

The service listens on `PORT` and supports these environment variables:

- `SPORTMONKS_API_KEY`
- `SPORTMONKS_SEASON_ID`
- `ALLOWED_ORIGINS`
- `PORT`
